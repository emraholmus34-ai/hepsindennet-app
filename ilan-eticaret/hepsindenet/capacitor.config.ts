import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.hepsindenet.app',
  appName: 'hepsindenet',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
}

export default config

import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hepsindenet.app',
  appName: 'hepsindenet',
  webDir: 'dist'
};

export default config;
